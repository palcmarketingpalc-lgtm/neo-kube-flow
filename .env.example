# AWS API Gateway Configuration
# Replace with your actual AWS API Gateway endpoint URL for form submissions
VITE_AWS_API_GATEWAY_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/prod/contact
